{
  "_comment": "Single-agent (fast) world. world-agent is the sole narrator/router; it computes divergence + drift itself and persists worldline_state + timeline each turn. Self-contained; key via DEEPSEEK_API_KEY env — never stored here.",
  "llm": {
    "model_ref": "deepseek",
    "max_retries": 3,
    "max_context_chars": 100000
  },
  "tui": {
    "narrator_agent_id": "world-agent"
  },
  "rules": {
    "enable_resolve_action": false
  },
  "models": {
    "stub": {
      "provider": "stub",
      "model": "stub"
    },
    "deepseek": {
      "provider": "openai",
      "model": "deepseek-v4-pro",
      "base_url": "https://api.deepseek.com/v1",
      "api_key_env": "DEEPSEEK_API_KEY"
    }
  }
}
