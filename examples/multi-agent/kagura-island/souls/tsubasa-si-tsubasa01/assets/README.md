# assets/ — 角色立绘 / character art

把这个角色的立绘放这里,命名 `portrait.png`(也支持 avatar / cover / face / card · .jpg / .webp）。

- 在 `neonrp web` 的右栏 agent lane 里作为头像显示;无图时回退到名字首字。
- 卡片自带:cast 进世界时,这张图随 soul 文件夹一起复制过去。
